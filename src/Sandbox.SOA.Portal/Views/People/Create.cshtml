@model Sandbox.SOA.Common.Contracts.People.PersonInfo

@{
    ViewBag.Title = "Person Create";
}

@section breadcrumb{

    @(Html.Breadcrumb()
          .Add("Home", Url.Home())
          .Add("People", Url.People())
          .Add("Create", Url.PersonCreate())
          .Render())
}

@Html.Partial("CreateForm")
