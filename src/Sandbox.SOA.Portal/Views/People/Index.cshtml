@using Sandbox.SOA.Portal.Helpers
@model Sandbox.SOA.Portal.Models.Person.PersonGridViewModel

@{
    ViewBag.Title = "People";
}

@section breadcrumb{

    @(Html.Breadcrumb()
          .Add("Home", Url.Home())
          .Add("People", Url.People())
          .Render())
}

@Html.Partial("IndexForm")
