@using Sandbox.SOA.Portal.Helpers
@model Sandbox.SOA.Portal.Models.People.PersonGridViewModel

@{
    ViewBag.Title = "People";
}

@section breadcrumb{

    @(Html.Breadcrumb()
          .Add("Home", Url.Home())
          .Add("People", Url.People())
          .Render())
}

@Html.Partial("IndexForm")
