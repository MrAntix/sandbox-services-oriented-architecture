@model IEnumerable<BreadcrumbHelper.Crumb>

<nav>
    <ol class="breadcrumb">
        @foreach (var crumb in Model)
        {
            <li>
                <a href="@crumb.Url" class="@(crumb == Model.Last() ? "active" : null)">@crumb.Text</a>
            </li>
        }
    </ol>
</nav>
