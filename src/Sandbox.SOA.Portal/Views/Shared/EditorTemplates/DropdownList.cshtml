@using Sandbox.SOA.Portal.Helpers
@model Sandbox.SOA.Portal.Models.People.DropdownList

@{ 
    Layout = "_Layout.cshtml";

    var htmlAttributes = ViewData
        .Extract("placeholder")
        .Include("class", "form-control");
}

@Html.DropDownList("Value", ViewData.GetList(), ViewData.GetListOption(), htmlAttributes)